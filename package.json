{
  "name": "bep20-usdt-payment-receiver",
  "version": "1.0.0",
  "private": true,
  "description": "BEP20 USDT payment receiver with invoice business logic",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:bsc": "hardhat run scripts/deploy.js --network bsc",
    "deploy:bscTestnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "invoice:create:bsc": "hardhat run scripts/createInvoice.js --network bsc",
    "serve:web": "npx serve web -l 4173"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.2"
  }
}
